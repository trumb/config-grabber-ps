# devices.txt - List of switch IP addresses to connect to
# One IP address (or hostname) per line.
# Lines starting with '#' are treated as comments and ignored.
# Blank lines are also ignored.
#
# PAT/NAT environments:
#   Use IP:PORT notation to specify a per-device SSH port.
#   If no port is given, the -Port argument is used (default: 22).
#
# Usage:
#   .\config-grabber.ps1 -DeviceFile examples\devices.txt -CommandFile examples\commands.txt -Username admin

# Core switches - standard port 22 (uses -Port default)
192.168.1.1
192.168.1.2

# Distribution switches - PAT'd through a firewall on custom ports
10.0.0.1:2221
10.0.0.2:2222

# Access switches - mix of standard and PAT'd
172.16.5.10
172.16.5.11:22100
172.16.5.12:22101
